larg = float(input('Digite a largura da parede: '))
alt = float(input('Digite a altura da parede: '))

area = (larg * alt)
litro = (area / 2)

print(f'A área total da parede a ser pintada equivale a {area}m², sendo assim necessário usar {litro}L de tinta.')

